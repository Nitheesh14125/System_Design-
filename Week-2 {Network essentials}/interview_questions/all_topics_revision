Question 1
Explain what happens when a user types google.com in a browser and presses Enter.

Score: 7 / 10

Your Answer (summary of what you wrote):
You explained that the browser first performs DNS resolution to convert google.com into an IP address. The browser checks browser cache, then OS cache, then a DNS resolver which queries the root server, TLD server, and authoritative server to get the IP address. After the IP is found, the browser connects to the server. You mentioned HTTP request and the TCP 3-way handshake (SYN, SYN-ACK, ACK) before data transfer and browser rendering.

Evaluation:
You correctly included:

DNS resolution flow

DNS hierarchy (root → TLD → authoritative)

TCP 3-way handshake

HTTP request

Server response and rendering idea

However, a few points need correction or clearer ordering:

TCP connection happens before the HTTP request, not after.

The 3-way handshake is used to establish the connection, not to transfer data.

The server sends HTTP response (HTML, CSS, JS), which the browser renders.

Correct Interview Answer (clean version):

The user types google.com in the browser.

The browser checks its DNS cache for the IP address.

If not found, the request goes to the OS cache.

If still not found, the request is sent to a DNS resolver.

The resolver queries:

Root DNS server

TLD server (.com)

Authoritative DNS server

The authoritative server returns the IP address of google.com.

The browser now establishes a TCP connection with the server using a 3-way handshake:

SYN

SYN-ACK

ACK

After the connection is established, the browser sends an HTTP request (for example, GET /).

The server processes the request and sends an HTTP response containing HTML, CSS, and JavaScript.

The browser renders the webpage for the user.

Good answer overall. The flow understanding is mostly correct, which is the main goal in system design basics.


Question 2
What is the difference between TCP and UDP, and give two real-world use cases for each.

Score: 7.5 / 10

Your Answer (summary):
You explained that TCP is a connection-oriented protocol that establishes a connection before data transfer, ensures ordered delivery, supports error correction, and guarantees reliable transmission using mechanisms such as the 3-way handshake. You also mentioned that it is slower and used for tasks like webpage loading and file downloading.

For UDP, you explained that it does not establish a connection, does not guarantee delivery or ordering, and is faster. You gave examples such as video streaming.

Evaluation:
Your conceptual understanding is good. You correctly identified:

Connection-oriented vs connectionless

Reliability differences

Error handling in TCP

Speed advantage of UDP

Real-world usage like streaming

However, a few corrections:

UDP is connectionless, not connection-oriented.

TCP 3-way handshake establishes connection, it does not transfer data itself.

Webpages themselves use HTTP over TCP, not directly TCP for images/video.

UDP examples could be expanded beyond streaming.

Correct Interview Answer (clean version):

TCP (Transmission Control Protocol) is a connection-oriented protocol that establishes a connection between client and server before data transmission. It ensures reliable communication through acknowledgments, retransmissions, and ordered delivery of packets.

Characteristics of TCP:

Connection-oriented

Reliable data delivery

Ordered packet transmission

Error detection and retransmission

Slower due to overhead

Real-world uses of TCP:

Web browsing (HTTP / HTTPS)

File downloads (FTP)

Email communication (SMTP)

UDP (User Datagram Protocol) is a connectionless protocol that sends data without establishing a connection. It does not guarantee delivery, ordering, or error correction, which makes it faster and suitable for real-time applications.

Characteristics of UDP:

Connectionless communication

No acknowledgment system

No guaranteed delivery

Faster with minimal overhead

Real-world uses of UDP:

Video streaming

Online gaming

Voice calls (VoIP)

Good performance. You clearly understand the core networking concepts.